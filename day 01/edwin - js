var inputRaw = await fetch(`https://adventofcode.com/2022/day/1/input`).then(x => x.text());

var inputPerElf = inputRaw.split('\n\n').map(x => x.split('\n'));
Math.max(...inputPerElf.map(x => x.reduce((agg, y) => agg += +y, 0)));
